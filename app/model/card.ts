export class Card {
    constructor (
        id: number,
        nombrePlato: string,
        foto: string,
        descripcion: string,
        precioRacion: number,
        activado: string,
        id_categoria: number
    ){}
}